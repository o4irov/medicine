@model лр5.Models.Hospital_need_medicine

@{
    ViewBag.Title = "Удалить";
}

<h2>Удалить</h2>

<h3>Вы действительно хотите удалить эту запись?</h3>
<div>
    <h4>Больницам требуются лекарства</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.quantity_of_packs)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.quantity_of_packs)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Hospital.addresHospital)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Hospital.addresHospital)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Medicine.name_of_medicine)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Medicine.name_of_medicine)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Удалить" class="btn btn-default" /> |
            @Html.ActionLink("Назад", "Index")
        </div>
    }
</div>
